function friend(friends) {
    //your code here
    let f = [];
    for (let i = 0; i < friends.length; i++) {
        if (friends[i].length == 4) {
            f.push(friends[i]);
        }
    }
}